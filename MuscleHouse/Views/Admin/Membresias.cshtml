@model IEnumerable<MuscleHouse.Models.Membresia>
@{
    ViewData["Title"] = "Consulta de Membresías - Admin";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2><i class="bi bi-card-heading me-2"></i>Historial de Membresías</h2>
</div>

<div class="card card-dark p-3">
    <div class="table-responsive">
        <table class="table table-dark table-hover align-middle mb-0">
            <thead>
                <tr>
                    <th>Cliente</th>
                    <th>Plan</th>
                    <th>Fecha Inicio</th>
                    <th>Fecha Vencimiento</th>
                    <th>Precio Pagado</th>
                    <th>Estado</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var m in Model)
                {
                    <tr>
                        <td><strong>@(m.Cliente != null ? $"{m.Cliente.Nombre} {m.Cliente.Apellido}" : $"Cliente #{m.ClienteId}")</strong></td>
                        <td>@(m.Plan != null ? m.Plan.Nombre : "N/A")</td>
                        <td>@m.FechaInicio.ToShortDateString()</td>
                        <td>@m.FechaVencimiento.ToShortDateString()</td>
                        <td>$@m.PrecioPagado.ToString("F2")</td>
                        <td>
                            @if (m.Estado == "Activa")
                            {
                                <span class="badge bg-success">Activa</span>
                            }
                            else if (m.Estado == "Vencida")
                            {
                                <span class="badge bg-danger">Vencida</span>
                            }
                            else
                            {
                                <span class="badge bg-warning text-dark">@m.Estado</span>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
